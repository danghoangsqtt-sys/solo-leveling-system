package com.systemleveling.core.model

enum class QuestType {
    DAILY, WEEKLY, BOSS, PENALTY, EVENT, SIDE
}

enum class QuestRank {
    E, D, C, B, A, S
}

enum class QuestStatus {
    PENDING, IN_PROGRESS, COMPLETED, FAILED, EXPIRED
}
