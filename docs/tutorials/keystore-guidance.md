# Hướng dẫn quản lý Keystore và Chữ ký Ứng dụng

⚠️ **BẢO MẬT: TUYỆT ĐỐI KHÔNG ĐƯỢC PHÉP ĐẨY LÊN GITHUB** ⚠️

Dưới đây là thông tin quan trọng về các file cấu hình chữ ký của ứng dụng Solo Leveling:

- `KEYSTORE-CREDENTIALS.txt` : Xem file này để lấy mật khẩu.
- `solo-leveling.jks` : Đây là chữ ký danh tính của app.

> **Lưu ý:** Các file bảo mật trên đã được cấu hình loại trừ (ignore) trong file `.gitignore` để đảm bảo không bị push nhầm lên repository.
